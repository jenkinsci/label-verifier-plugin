<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout"
         xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:block>
    <f:hetero-list name="verifiers" items="${instance.verifiers}" descriptors="${descriptor.getVerifierDescriptors()}"
            addCaption="${%Add Verifier}" deleteCaption="${%Delete Verifier}"
            hasHeader="true" />
  </f:block>
</j:jelly>
